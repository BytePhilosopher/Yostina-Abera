# Base URL of the backend API.
# Leave empty for a full-stack deploy (Express serves the client on the same origin).
# Set this only if the frontend is hosted separately from the API,
# e.g. VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
